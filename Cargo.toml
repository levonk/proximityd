[package]
name = "proximityd"
version = "0.1.0"
edition = "2021"
description = "Generic presence detection service with pluggable notifications"
repository = "https://github.com/levonk/proximityd"
license = "MIT OR Apache-2.0"
keywords = ["bluetooth", "ble", "presence", "notifications", "discord", "home-automation"]
categories = ["command-line-utilities", "network-programming"]

[lib]
name = "btnotify"

[dependencies]
clap = { version = "4.4", features = ["derive", "env"] }
clap_complete = "4.4"
clap_mangen = "0.2"
anyhow = "1.0"
thiserror = "1.0"
directories = "5.0"
indicatif = "0.17"
glob = "0.3"
atty = "0.2"
num_cpus = "1.16"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json", "ansi"] }
tracing-appender = "0.2"
chrono = { version = "0.4", features = ["serde"] }
toml = "0.8"
ansi_term = "0.12"
once_cell = "1.18"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "signal", "process"] }
async-trait = "0.1"
futures = "0.3"
btleplug = "0.11"
reqwest = { version = "0.12", features = ["json", "blocking"] }
rusqlite = { version = "0.32", features = ["bundled"] }
rumqttc = { version = "0.24", optional = true }
ratatui = "0.26"
crossterm = "0.27"
regex = "1.10"
pager = "0.16"
textwrap = "0.16"

[features]
default = []
mqtt = ["rumqttc"]

[target.'cfg(target_os = "linux")'.dependencies]
geoclue-zbus = "0.1"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.0"
tempfile = "3.8"
